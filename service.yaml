apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: ai-photo-studio-api
  annotations:
    run.googleapis.com/ingress: all
spec:
  template:
    metadata:
      annotations:
        run.googleapis.com/cpu-throttling: "false"
        run.googleapis.com/execution-environment: gen2
        run.googleapis.com/cloudsql-instances: project-9540c255-c960-4fa0-a91:us-central1:ai-photo-studio-db
        autoscaling.knative.dev/minScale: "1"
        autoscaling.knative.dev/maxScale: "10"
    spec:
      serviceAccountName: github-actions-deploy@project-9540c255-c960-4fa0-a91.iam.gserviceaccount.com
      containers:
      - image: us-central1-docker.pkg.dev/project-9540c255-c960-4fa0-a91/ai-photo-studio-api/ai-photo-studio-api:latest
        ports:
        - containerPort: 3000
        resources:
          limits:
            cpu: "1"
            memory: 512Mi
        env:
        - name: NODE_ENV
          value: production
        envFrom:
        - secretRef:
            name: cloudrun-secrets
      timeoutSeconds: 300
      containerConcurrency: 80
