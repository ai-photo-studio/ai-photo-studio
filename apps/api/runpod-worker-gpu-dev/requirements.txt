# GFPGAN GPU worker candidate pinned dependencies (build-test only)
# torch and torchvision are installed from the official PyTorch cu121 index
# in the Dockerfile, so they are NOT listed here (avoids CPU-wheel override).
numpy==1.26.4
opencv-python-headless==4.9.0.80
Pillow==10.2.0
gfpgan==1.3.8
facexlib==0.3.0
basicsr==1.4.2
PyYAML==6.0.1
tqdm==4.66.2
