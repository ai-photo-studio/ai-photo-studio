# Bank Alfalah MPGS — USD Currency Document Audit (R9.2-MERGE-P143-AND-ONE-USD-SANDBOX-DIAGNOSTIC)

**Status:** Currency document audit complete; the owner-authorized single
live USD sandbox request has now been made and classified (§6): one real
request, `currency=USD`, result `HTTP 401` — an authentication/
merchant-profile failure confirmed currency-independent, not a currency
issue. `P4C_MPGS_AUTH_VERIFIED` remains NOT achieved for either currency.
No new bank contact was made to produce the original audit (§1-4); it was
a synthesis of already-recorded, owner-reported bank communication and the
bank's own live-fetched V100 documentation, both already present in this
repository.

## 1. Evidence table

| Question | Finding | Source |
|---|---|---|
| Is MPGS (this bank's V100 Hosted Checkout integration) USD-only? | **No.** No document in this repository states or implies MPGS itself is USD-only. The only "single-currency" statement found anywhere in this evidence set belongs to a **different, retired** protocol (see §2 contradiction below), not the current MPGS V100 integration. | `p4c-bank-alfalah-legacy-apg-retired.test.ts`; `P4D_MPGS_FINAL_LOCAL_INVESTIGATION_2026-08-05.md` §comparison table |
| Does Bank Alfalah document PKR support? | **Yes, directly bank-confirmed.** PKR is the merchant's home-market settlement currency; enabled in code as `standard-pattern-fallback` (well-established Hosted Checkout REST pattern, not itself a currency-specific claim). | `p4c-bank-alfalah-mpgs-gateway.service.ts` `MPGS_CURRENCY_SUPPORT.PKR`; `MPGS_INTEGRATION_EVIDENCE.md` §6 |
| Does Bank Alfalah document USD support? | **Yes, directly bank-confirmed, owner-reported.** "The same credentials (Merchant ID + API Password) are valid for both PKR and USD sandbox testing on this merchant profile." This is recorded as `doc-confirmed-live-fetch` evidence in code (the strongest evidence tier this repository uses) because it is a direct statement from the bank, not an inference from generic docs. | `P4D_BANK_CONFIRMED_MERCHANT_PROFILE_2026-08-05.md` §1 item 4; `p4c-bank-alfalah-mpgs-gateway.service.ts` `MPGS_CURRENCY_SUPPORT.USD` |
| Is there an explicit `order.currency` ISO 4217 requirement documented? | **Not separately quoted from a bank-supplied document in this repository.** Every real and stub request already sends a 3-letter code (`PKR`/`USD`), consistent with the universal MPGS/Mastercard REST convention and with every `order.currency` example in the bank's own live-fetched V100 doc (both of which happen to use standard 3-letter codes) — but no fetched document in this repository states the ISO 4217 requirement as an explicit rule in words. Treated as a de facto, already-implemented convention, not a separately bank-confirmed fact. | `p4c-bank-alfalah-mpgs-gateway.service.ts` (`FixedOrderCurrency = "PKR" \| "USD"`, sent verbatim as `order.currency`); no explicit "ISO 4217" string found anywhere in `docs/payments/bank-alfalah-mastercard/` |
| Does currency support depend on merchant-profile enablement, independent of the currency itself? | **Yes.** The bank's own confirmation frames both PKR and USD as sandbox-testable "on this merchant profile" — i.e., a property of the profile's enablement state, not of either currency individually. The persistent `HTTP 401` (`"Invalid credentials"`) affecting the **PKR** leg (which has never been currency-disputed) is itself evidence that the open blocker is profile/credential-level, not currency-level. | `P4D_BANK_CONFIRMED_MERCHANT_PROFILE_2026-08-05.md` §1 item 4; `R9.2_MPGS_CI_LIVE_PROOF_2026-08-05.md` (PKR, `401`); `R9.2_MPGS_FINAL_CONTRACT_TABLE_AND_DRIFT_PROTECTION.md` §3 (byte-identical `401` for correct vs. deliberately-wrong password, i.e. rejected before/independent of credential correctness) |
| Any contradiction between bank emails, website, and gateway docs? | **Yes, one, already recorded and explicitly not silently reconciled.** A comparison document from an earlier packet quotes a **different, retired** Bank Alfalah protocol ("Alfa APG", not MPGS) as stating "Currency ... will always be PKR." That statement describes a protocol this repository has already fully retired (guarded by a permanent regression test) and is not a statement about the current MPGS V100 Hosted Checkout integration this repository uses. It is recorded here as a documented contradiction between two *different* Bank Alfalah integration generations, not as evidence that MPGS itself is PKR-only. | `P4D_MPGS_FINAL_LOCAL_INVESTIGATION_2026-08-05.md` (comparison row: "PKR/USD capability \| PKR only (\"Currency ... will always be PKR\")" — explicitly the *retired Alfa APG* column, contrasted against the current MPGS adapter's independent, bank-confirmed-for-both currency gating in the same row) |

## 2. The one contradiction, in full

`P4D_MPGS_FINAL_LOCAL_INVESTIGATION_2026-08-05.md` independently compared the
**retired** Alfa APG integration guide against the **current** MPGS V100
adapter, field by field. One row reads:

| Field | Retired Alfa APG (legacy, different protocol) | Current MPGS adapter (this repository) |
|---|---|---|
| PKR/USD capability | PKR only ("Currency ... will always be PKR") | PKR enabled (`standard-pattern-fallback`); USD gated separately, bank-confirmed for sandbox testing only |

This is the sole place anywhere in this repository's evidence where a
PKR-only claim appears. It describes a **different, already-retired**
protocol (Alfa APG — see `p4c-bank-alfalah-legacy-apg-retired.test.ts`, which
permanently guards against that protocol's reintroduction) and is recorded,
not reconciled, per this repository's standing practice of never silently
resolving a documented contradiction. It is **not** evidence that the
current MPGS integration — the one this packet tests — is PKR-only.

No document anywhere in this evidence set claims MPGS is **USD-only**
either. This audit did not infer USD-only status merely because some
generic MPGS/Mastercard documentation elsewhere in the industry commonly
uses USD as its example currency in code samples — no such inference was
drawn or relied upon anywhere in this repository's code or evidence; USD
enablement here rests exclusively on the bank's own direct, merchant-profile
-specific confirmation (§1 above), never on a generic third-party example.

## 3. Conclusion driving this packet's USD dry-run and live diagnostic

- `MPGS_CURRENCY_SUPPORT.USD.enabled = true` (evidence tier
  `doc-confirmed-live-fetch`, the strongest tier used in this codebase) is
  already correct and required no code change.
- The one open blocker (`P4C_MPGS_AUTH_VERIFIED` not achieved, `HTTP 401` on
  the PKR leg) is profile/credential-level per the evidence above, not
  currency-level — so a USD attempt is expected, per this packet's own
  classification rule (§5 of the task), to either bypass it (if the
  blocker were somehow currency-specific, which no evidence supports) or
  reproduce the same `401` (confirming it is currency-independent, as the
  evidence in this document already strongly suggests).
- Neither PKR nor USD is `SANDBOX_VERIFIED` as of this document.

## 4. Permanent protection

See `rules.md` for a new permanent rule: example currency values in generic
third-party MPGS/Mastercard documentation must never be treated as
merchant-profile currency authority. Only a bank-specific, merchant-profile
-specific confirmation (an email, a portal statement, or a successful
bounded sandbox test) may enable or disable a currency in
`MPGS_CURRENCY_SUPPORT`.

## Protected scope

No live Bank Alfalah request was made to produce this document. No card
data, no capture, no production credential, no RunPod/Replicate/R2/webhook/
deployment change. No code was changed by this document itself (the code
changes proving USD in this same packet are in
`mpgs-actual-app-dryrun.spec.ts` and the live workflow/spec, documented
separately).

## 5. Update (R9.2-CANCEL-WRONG-RUN-MERGE-P144-AND-WATCH-USD-PROOF, 2026-08-06)

### 5.1 A "wrong run" was found and classified, not a new USD request

GitHub Actions run
[`31058730527`](https://github.com/ai-photo-studio/ai-photo-studio/actions/runs/31058730527)
was inspected: `workflow_dispatch`, branch `main`, SHA
`e05d04a5b46edffb9fc68ebc09ea9803c9e05a98` (the PR #143 merge commit —
**before** PR #144 added the `currency` input), created
`2026-08-06T00:08:43Z`. By the time it was inspected the run had already
**completed successfully**, including its own "Assert exactly one real
gateway call was made" step — it was not queued or running, so there was
nothing to cancel.

Because this run's workflow definition predates the `currency` input
entirely, it silently ran the old, unconditional PKR path regardless of
any dispatch-time intent. Downloaded evidence confirms:

```json
{"level":"info","message":"Bank Alfalah MPGS: initiateHostedCheckout request","time":"2026-08-06T00:11:25.655Z","meta":{"method":"POST","path":"/session","apiVersion":"100","currency":"PKR","orderIdLength":20}}
{"level":"warn","message":"Bank Alfalah MPGS: initiateHostedCheckout failed","time":"2026-08-06T00:11:26.050Z","meta":{"status":401,"detail":"content-type=application/json;charset=ISO-8859-1 www-authenticate=none correlation-id=none"}}

initiateHostedCheckout call count: 1
```

**Currency: PKR. Result: HTTP 401 ("Invalid credentials"), identical
pattern to every prior PKR live attempt.** All three evidence screenshots
(`baf-live-before-click.png`, `baf-live-after-click.png`,
`baf-live-gateway-sanitized.png`) were visually inspected: real order
review page (Market PAKISTAN, Tier Original, Amount PKR 250.00), the
truthful `401` error surfaced in the UI (no fabricated success), and the
rendered sanitized summary — no password, `Authorization` header, or any
other secret value visible in any of the three.

This is **not** the owner-authorized USD request and does not consume or
satisfy it — it is a second, independent PKR live attempt (the first was
run `31042211650`, `R9.2_MPGS_CI_LIVE_PROOF_2026-08-05.md`), and it
further reinforces this document's §1 finding that the open blocker is
merchant-profile/credential-level and currency-independent: two separate
PKR attempts, months apart in this evidence set's terms, both `401`,
identical failure shape.

### 5.2 Updated `main` re-verified after PR #144 merge

PR #144 merged (`14a745b9274f3d6a23f03ce11ecb4be2c76cee3d`). A **fresh**
worktree from updated `origin/main` (not the PR branch) confirmed
statically and by live execution: `workflow_dispatch` accepts
`currency: PKR | USD` (default `PKR`, unchanged); the live job wires
`LIVE_TEST_CURRENCY` from the input; USD selects country "US"
(`INTERNATIONAL` market); the live spec asserts the real PriceBook amount
`USD 1.50`; USD-specific evidence filenames
(`baf-usd-before-click.png`, `baf-usd-result.png`,
`baf-usd-gateway-sanitized.png`) are present in the artifact upload list
alongside the unchanged PKR filenames; the "exactly one real gateway
call" assertion and the API-password redaction step are both intact,
unmodified. The full actual-app dry-run suite was re-run from this fresh
`main` worktree end to end (real disposable Postgres, real stub gateway,
real API, real web app): **7/7 passed, exit 0.**

### 5.3 New permanent protection

See `rules.md`: a `workflow_dispatch` for a workflow whose parameters were
just added must be dispatched from a ref that actually contains those
parameter definitions — dispatching from an older ref silently drops
inputs the running workflow copy doesn't define, rather than erroring,
and can consume a rationed live request on the wrong (default) path
without any warning.

## 6. The owner-authorized live USD sandbox request (run `31061334403`, 2026-08-06)

The owner dispatched
[`31061334403`](https://github.com/ai-photo-studio/ai-photo-studio/actions/runs/31061334403):
`workflow_dispatch`, branch `main`, SHA
`14a745b9274f3d6a23f03ce11ecb4be2c76cee3d` (the updated `main` HEAD
verified in §5.2, containing the `currency` input). The `dry-run` job was
`skipped` (correct — dispatched with `mode=live`); the `live` job ran
exactly once and completed successfully end to end, including its own
"Assert exactly one real gateway call was made" step. No `workflow_dispatch`
run has occurred since.

### 6.1 Proof of exactly one USD request and its exact contract

Confirmed independently via the downloaded `api-server.log` (not just the
sanitized summary): exactly one `"Bank Alfalah MPGS: initiateHostedCheckout
request"` line and one matching `"...failed"` line, no others, no
`GET .../order/...` (Retrieve Order) line at all.

```json
{"level":"info","message":"Bank Alfalah MPGS: initiateHostedCheckout request","time":"2026-08-06T00:59:08.306Z","meta":{"method":"POST","path":"/session","apiVersion":"100","currency":"USD","orderIdLength":20}}
{"level":"warn","message":"Bank Alfalah MPGS: initiateHostedCheckout failed","time":"2026-08-06T00:59:08.517Z","meta":{"status":401,"detail":"content-type=application/json;charset=ISO-8859-1 www-authenticate=none correlation-id=none"}}

initiateHostedCheckout call count: 1
```

Contract, cross-checked against the unchanged adapter code (§5.2's static
verification, and the existing unit/dry-run test suites which assert
these exact literal values byte-for-byte): `POST
/api/rest/version/100/merchant/{merchantId}/session`, Basic Auth
`merchant.<merchantId>`, `apiOperation=INITIATE_CHECKOUT`,
`interaction.operation=PURCHASE`, `interaction.merchant.name` set,
`interaction.returnUrl` environment-owned (never client-supplied),
`order.currency=USD`, `order.amount` server-owned (real PriceBook, not
client input), `order.id` server-generated, 20 characters (well under the
30/41-char limits). No PKR request, no card data, no payment, no capture,
no Retrieve Order call, no retry — confirmed by the single request/failure
pair above and by the real order id `FO-MSGT5ODO-308A59D7` appearing
exactly once in the full log.

### 6.2 Screenshot, trace, and secret-redaction review

All three required screenshots downloaded and visually inspected:

- `baf-usd-before-click.png` — real order review page, Market
  **INTERNATIONAL**, Tier Original, **Amount USD 1.50** (real, unmodified
  PriceBook `PB-2026-08-03-v1`), order `FO-MSGT5ODO-308A59D7`, "Pay
  securely" visible. No secret visible.
- `baf-usd-result.png` — same page, truthful `"Bank Alfalah MPGS
  initiateHostedCheckout failed with status 401 ..."` surfaced in the UI;
  no fabricated paid/success state. No secret visible.
- `baf-usd-gateway-sanitized.png` — rendered sanitized summary, matches
  §6.1's log lines exactly. No password or `Authorization` value visible.
- Playwright trace (`trace.zip`, 580KB, valid archive) reviewed: the
  browser's own network trace (`0-trace.network`) contains **zero**
  references to the real bank host — confirming the browser itself never
  rendered or contacted `test-bankalfalah.gateway.mastercard.com`; only
  the one real server-side request (proven above) ever left the runner
  toward the bank.
- Full `api-server.log` reviewed line by line: no password, API key, or
  `Authorization` header value appears anywhere (the adapter's
  observability logging never emits response bodies or auth headers, by
  design — confirmed unchanged).

### 6.3 Classification: `401` — currency-independent authentication/profile failure

Per this packet's classification rule: **`session.id` was never
returned; result is `HTTP 401`.** This is classified as an
authentication/merchant-profile access failure, **not** a currency-level
issue. Evidence: identical failure shape (`401`, same header pattern, no
`WWW-Authenticate`, no correlation id) as both prior PKR live attempts
(runs `31042211650` and `31058730527`) — three independent live requests
across two currencies, all rejected identically before any
currency-specific logic could matter. **PKR versus USD is confirmed not
the cause.** `P4C_MPGS_AUTH_VERIFIED` remains NOT achieved for either
currency. Neither PKR nor USD is `SANDBOX_VERIFIED`.

### 6.4 Exact next action

Per this classification, the final short bank support email (requesting
API password reissue and REST permission confirmation) is the next
step — drafted, **not sent**, pending owner review, at
`D:\Temp\claude\evidence\baf-final-support-email-2026-08-06.txt` (outside
the repository; contains no secret). This repeats and reinforces the
standing recommendation already recorded in
`R9.2_MPGS_SANDBOX_TO_PRODUCTION_PROCEDURE.md` and the final support
packet from an earlier session — this packet did not send it, per the
task's explicit scope (no bank contact, no production credential
request). **No further live sandbox request should be made until the
bank confirms the credential/profile is reissued and REST-API-enabled**;
re-attempting without that would not produce new information (three
identical `401`s already rule out every client-side variable).
