# Restoration System

## Production Standard

`originals/` upload -> Sharp normalization -> Flux Restore -> GFPGAN v1.4 scale 1 -> Sharp Master/Preview/2HD/4HD -> R2. Standard is unchanged by Premium foundations.

## Storage Mapping

- Original: `RestorationItem.originalStorageKey` (`originals/`).
- Flux/GFPGAN: in-memory provider outputs; prediction metadata only, no standalone R2 intermediate key.
- Master/final: `finalStorageKey` and `metadata.restorationOutputs.master.key` (`finals/`).
- Preview: `previewStorageKey` (`previews/`).
- 2HD/4HD: `metadata.restorationOutputs.variants` (`finals/`).

## Comparison, Entitlement, Payment

Before is the signed original key; After is signed final key. Missing original shows an error. Downloads require guest/user ownership plus explicit purchased tier. Payment alone does not unlock a legacy tier. No provider processing or processed preview before confirmed payment.

## Disabled/Proposed

Damage masks are Sharp-decoded grayscale PNGs and disabled by `RESTORATION_DAMAGE_MASK_ENABLED=false`. Face gate is disabled by `RESTORATION_FACE_GATE_ENABLED=false`. Premium Reconstruction is a separate disabled contract; it requires validated mask, preservation instruction, payment, and quality acceptance. `UnifiedLocalRestorationProvider` is unsafe/quarantined.

Premium routing is Standard at 0-39, Standard or Premium at 40-79, and Premium-only at 80-100. Severe face loss or excessive mask coverage is Premium-only. Calibration evidence remains incomplete.

Calibration intake: run `npm run restoration:fixture-intake -- --id <id> --category <category> --original <path>` with optional `--mask`, `--flux`, `--gfpgan`, and `--final`. Intake validates local image decoding/checksums but does not infer categories or identity metrics.

Run `npm run test:restoration:review-queue` to generate a local operator contact sheet and evidence queue. Only trace-backed groups are registered; filename-only candidates remain ungrouped.

RunPod is disabled by default and benchmark-only. The isolated worker supports health and local dry-run metadata; it is not connected to Standard, Premium, or production routing.

Gate 1 remains build-only validation and does not publish images. Gate 2 is explicitly approved once per run for one development-only immutable GHCR image, via manual workflow dispatch on the default branch, using the validated full SHA and registry digest evidence. Gate 2 was consumed on 2026-07-31 by publishing the GPU candidate as `ghcr.io/ai-photo-studio/ai-photo-studio/runpod-worker-gpu-dev:f65088b5f6bb2f5a91b8b877b32f032766c8b5f1` at digest `sha256:049a304b44bec75562a74eac3f5be312feacd6133da80a0dc86d0a136a86a63a` (source SHA `f65088b5f6bb2f5a91b8b877b32f032766c8b5f1`, subtree `ea8a583e5d7279c0b67eec66a1906b7523c4ce99`); publicationAllowed is now false and not reusable; any candidate change requires a new explicit Gate 2 approval. Gate 3 canary readiness references the published handler image digest `sha256:1a74aefec1a7f77ebdbf7fd19ba2b9a816600f1e3d43ac7ce10b3b87367a3895`; the registry access is resolved as PUBLIC (anonymous pull succeeded, run 30652207024) and a deterministic synthetic canary fixture is verified offline (run 30653164933; PNG `f4368b08...`, input `b2e6c6a2...`, output `99def1c3...`, faces=1). The Gate 3 canary remains BLOCKED only on the GPU/volume region coexistence decision (`regionCompatibilityResolved: false`): read-only RunPod authorization was provided but `RUNPOD_API_KEY` is not set in the environment, so no read-only GET/list query could be authenticated (credential-unavailable; no key extracted from git/committed files). A separate unpublished handler-wrapper candidate now exists at `apps/api/runpod-worker-gpu-serverless-dev/` (`runpod.serverless.start` wrapper around the immutable CLI image); it is build-test only, contains no weights, and requires a new explicit Gate 2 approval before publication. Wrapper Gate 2 readiness is prepared (source frozen at `21e2921`, subtree `b9402fa9...`); publication must pin the exact source/subtree and one immutable tag, and the registry digest must be verified before Gate 2 closes. No model weights may be published; publication is not GPU or quality approval. Wrapper Gate 2 was CONSUMED on 2026-07-31 (published as `ghcr.io/ai-photo-studio/ai-photo-studio/runpod-worker-gpu-serverless-dev:21e292103979f0450dffafe09844fac3b435031b` at digest `sha256:1a74aefec1a7f77ebdbf7fd19ba2b9a816600f1e3d43ac7ce10b3b87367a3895`); the wrapper publicationAllowed is now false and not reusable; any wrapper change requires a new explicit Gate 2 approval. Gate 3 RunPod canaries and Gate 4 production activation remain prohibited unless separately approved.

Legacy coupled RunPod tests are superseded by current-main-native tests. Gate 3 cannot start from documentation alone; explicit user approval, verified rate, fixed budget, endpoint/template, and one-job limit are mandatory. Manifest defaults remain fail-closed. No secret may be committed. The RunPod template must pin the immutable digest. Gate 4 remains separately prohibited. Replicate remains production. Successful health/dry_run alone is not restoration-quality approval.

GFPGANv1.4 weight provenance is verified via GitHub Actions (run 30618746285): size 348632874 bytes, SHA-256 e2cd4703ab14f4d01fd1383a8a8b266f9a5833dacee8e6a79d3bf21a1b6be5ad, checksum source independently-calculated. Apache-2.0 applies to source code only; it does not prove weight redistribution rights. No model binary may be committed; no weight may be bundled without explicit redistribution evidence; external weights must match the pinned checksum before startup; runtime network weight download remains prohibited. A separate unpublished GFPGAN GPU candidate exists at `apps/api/runpod-worker-gpu-dev/` (build-test only) that mounts the weight externally and validates size and SHA-256 before model load; it is not published and GPU execution is unverified. Its build-only CI passed baseline (run 30620758562, image ID sha256:bf6af925ca2d4e3ef9c877a5fcde84907f30ad917e17f0e5591ef907081a8846, baseline size 5522182156 bytes, PyTorch 2.1.2+cu121). The candidate was hardened for Gate 2 readiness: base image pinned by immutable digest, non-root runtime user, build tools removed, caches purged, OCI labels added, SBOM and vulnerability scan performed (findings only; no upload). Hardened CI run 30622042430: size 5454210979 bytes (baseline 5522182156), SBOM 223 packages, vuln scan 35 (MEDIUM 15, HIGH 19, CRITICAL 1) with exact critical blocker CVE-2025-32434 in torch 2.1.2 (fixed in torch >= 2.6.0); not silently ignored. A minimal security upgrade to torch 2.6.0 + torchvision 0.21.0 was investigated and REJECTED (evidence run 30624931147): torchvision 0.21 removed `functional_tensor`, and BasicSR 1.4.2 (latest release) imports it, so the official gfpgan/basicsr stack is incompatible without an unreleased BasicSR pin or a prohibited patch. The official BasicSR fix commit `8d56e3a0` (PR #650) was then evaluated as a reproducible candidate (workflow run 30625934026): it resolves the `functional_tensor` import and GFPGANer constructs under torch 2.6.0+cu126 / torchvision 0.21.0+cu126, but adoption is NOT recommended because that commit is 18 commits / 41 files ahead of v1.4.2 (unrelated substantive changes; the wheel is not minimal) and GFPGANer construction triggers runtime network downloads of facexlib weights. Source archive SHA-256 `88a422325c7a08a9f3b6109e747bef5fbdf85d884d6033eacaf11f6c374aade9`; licence Apache-2.0. The candidate remains on torch 2.1.2+cu121 with CVE-2025-32434 documented. Weight remains external and checksum-pinned. No image has been published; floating tags and checksum drift are prohibited. Build success is not GPU inference or quality approval. The CPU worker remains unchanged. Separate explicit Gate 2 publication approval is mandatory; after publication, separate Gate 3 approval is mandatory. A tracked explicit one-line BasicSR v1.4.2 patch (functional_tensor->functional) was evaluated (run 30627778687): patch SHA-256 834cf12b1e625ce59ec2af4152421d2c6c113b8f0a54ba4544f8937b014289ac, source archive f23fe3558fff4cc038186ffd417d69d8bc1fd0eea2a9c755c401e9dfecc18152; it resolves the import and GFPGANer constructs under torch 2.6.0+cu126/torchvision 0.21.0+cu126, but the wheel build is blocked on a CUDA-less runner and GFPGANer still runtime-downloads facexlib auxiliary weights (detection_Resnet50_Final.pth, parsing_parsenet.pth). Both auxiliary weights have independent SHA-256 only (no publisher digest) and redistribution is unverified. Candidate dependencies remain unchanged; adoption not approved; runtime download and weight bundling prohibited. Fully offline GFPGAN construction and safe loading of all three externally mounted, checksum-verified weights was proven in a `--network none` container (workflow run 30632558271, `GFPGANer constructed; main GFPGAN weight loaded`, tiny CPU contract inference faces: 1, `gpu_inference_executed: false`). Independent hashes are technical integrity pins, not publisher-signed and not redistribution permission; external/BYO mounting is the only candidate packaging mode. `offlineConstructionVerified: true`; candidate unchanged; adoption not approved; publication, runtime download and weight bundling remain prohibited. The proven torch 2.6 stack was adopted into the unpublished candidate (torch 2.6.0+cu126 / torchvision 0.21.0+cu126, gfpgan 1.3.8, facexlib 0.3.0, and BasicSR v1.4.2 source f23fe355... with tracked patch 834cf12b... applied at build). The worker validates all three external weights (main + detection + parsing) and enforces TORCH_FORCE_WEIGHTS_ONLY_LOAD=1. Candidate adopted but unpublished; build/CPU inference is not GPU-quality approval; separate Gate 2 publication approval and later Gate 3 canary approval remain mandatory. Gate 3 and Gate 4 remain prohibited. Candidate source is frozen pending the Gate 2 decision; any candidate code/dependency change invalidates the current Gate 2 evidence and requires a new explicit Gate 2 approval. Any future publication must pin an immutable source SHA and a single immutable tag (no latest/dev floating tag); the published digest must be verified before Gate 2 closes; weights must never be bundled; publication is not GPU execution or quality approval.

A read-only, build-test-only mount-path audit (`.github/workflows/mount-path-audit.yml`, run `30659523185`) proved that the currently published handler image (digest `sha256:1a74aefec1a7f77ebdbf7fd19ba2b9a816600f1e3d43ac7ce10b3b87367a3895`) cannot load weights from `/runpod-volume`, the RunPod Serverless Network Volume mount point: synthetic fixtures mounted there were invisible via `/models` (`weightPresent`/`auxDetectionPresent`/`auxParsingPresent` all `false`), while a control mount directly at `/models` succeeded, and no `/runpod-volume` reference exists anywhere in the worker/handler source. A separate unpublished correction candidate, `apps/api/runpod-worker-gpu-serverless-volume-dev/`, was created, frozen at source commit `158bbbcb82b02db0ca0a2e66bc88a2ccfdb6e374` (subtree `716fc09fc35bb966637bef26f3d4840a7088891b`), deriving directly from the same published handler digest and adding exactly one build-time change: a fixed symlink `/models -> /runpod-volume/models` (no init script, no handler/CMD/entrypoint/dependency change). Build-only CI (run `30660299770`, confirmed via `gh run view --json headSha` to have tested this exact frozen commit) passed all mount-contract tests (valid mapping, missing-volume fail-closed, invalid-weight fail-closed, non-root security), zero CRITICAL, CVE-2025-32434 absent, SBOM 324 components, image ID `sha256:d66479f1e8e009b7379ac32a30d6c06789f6d687b6cb0a1cdff0be7c935ddc1d`, size `6751978537` bytes. Gate 2 readiness packet was created for this candidate (`RUNPOD_VOLUME_HANDLER_GATE2_APPROVAL_PACKET.md`, `runpod-volume-handler-gate2-readiness.json`). Volume-mapped handler Gate 2 was APPROVED (explicit user authorization, 2026-07-31T22:51:08Z) and CONSUMED by publication (workflow runs 30671211535 + fix + 30672004615). Published as `ghcr.io/ai-photo-studio/ai-photo-studio/runpod-worker-gpu-serverless-volume-dev:158bbbcb82b02db0ca0a2e66bc88a2ccfdb6e374` at digest `sha256:29ca5aa0aae46ab03719c52ae25fa98a61830adbbc8b317bd244ffb7ff837d9b`. All post-publication verification tests PASS (non-root user, symlink target verified inside container, handler path, no weights). `publicationAllowed` is now `false` and not reusable; any source/base/dependency change requires a new explicit Gate 2 approval. Network Volume creation requires separate, explicit mutation/cost authorization not covered by any publication approval. Under one-time dispatch authorization, `runpod-create-gate3-volume.yml` was dispatched exactly once (run `30674708510`, 2026-08-01T00:07:14Z) and FAILED at the preflight step; the Create Network Volume step and everything after it show `status: skipped`, so no `POST /v1/networkvolumes` call was made and no RunPod resource, endpoint, template, worker, or job of any kind was created (compute cost $0.00). Root cause: the preflight step used an obsolete GraphQL contract (a nonexistent root `datacenters` field and an invalid `gpuCount` argument on `gpuTypes`), a workflow-implementation defect, not an authorization or credential issue. This was corrected in a narrow follow-up PR: datacenter/GPU discovery now uses the current official `runpodctl` CLI (`runpodctl datacenter list`, `runpodctl gpu list --include-unavailable`) installed from a pinned GitHub release binary, and the volume-creation request body was corrected to match the official `NetworkVolumeCreateInput` schema (`name`, `size`, `dataCenterId` only, resolved dynamically from preflight — no hardcoded/unverified datacenter literal, no unsupported `tier` field). The corrected workflow has not been dispatched; a second dispatch requires new, separate, explicit authorization. Gate 3 still requires separate explicit approval after publication and volume preparation. Gate 3 and Gate 4 remain prohibited; Replicate remains production.

## RunPod Restoration Track Roadmap (as of 2026-08-01, evidence: this document's Gate history above)

- **Completed:** CPU/dev worker Gate 2 published and consumed (`runpod-worker-gpu-dev`, digest `sha256:049a304b...`). GPU CLI worker Gate 2 published and consumed (digest `sha256:049a304b44bec75562a74eac3f5be312feacd6133da80a0dc86d0a136a86a63a`). Handler wrapper Gate 2 published and consumed (digest `sha256:1a74aefec1a7f77ebdbf7fd19ba2b9a816600f1e3d43ac7ce10b3b87367a3895`). Volume-mapped handler Gate 2 published and consumed (digest `sha256:29ca5aa0aae46ab03719c52ae25fa98a61830adbbc8b317bd244ffb7ff837d9b`, publication runs 30671211535+30672004615, all post-publication tests PASS). GHCR registry access resolved PUBLIC. Synthetic canary fixture verified offline. Region/GPU read-only evidence gathered (BLOCKED: zero Network Volumes exist). Mount-path audit of published handler completed (BLOCKED: incompatible); volume-mapped handler resolves this blocker.
- **Current stage:** Volume-mapped handler candidate Gate 2 remains APPROVED, PUBLISHED, and CONSUMED (one-time, not reusable). The first authorized Network Volume creation dispatch (run `30674708510`) FAILED at preflight with no resource created (obsolete GraphQL contract); the workflow has been corrected (PR merging `runpodctl`-based discovery and the official `NetworkVolumeCreateInput` schema) and validated by an expanded security test suite, but the corrected workflow has not been dispatched. Awaiting new, separate, explicit authorization for a second dispatch.
- **Remaining stages, in order:** (1) explicit, separate authorization to dispatch the corrected Network Volume creation workflow; (2) verify exactly one Network Volume is created with the expected size/name/datacenter; (3) revalidate region/GPU/volume co-location evidence against the created volume; (4) explicit, separate Gate 3 approval for a single one-job canary with fixed budget and fail-closed limits; (5) canary execution and evidence capture; (6) Gate 4 (production activation) remains separately prohibited and is not currently planned.
- **Approximate completion:** ~75% of the path to a Gate-3-ready state (all build/publication work completed; workflow-repair complete; the single remaining blocker is explicit authorization for a second Network Volume creation dispatch — a mutation/cost decision, not engineering work). 0% of Gate 3 or Gate 4 execution has occurred.
