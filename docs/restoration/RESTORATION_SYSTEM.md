# Restoration System

## Production Standard

`originals/` upload -> Sharp normalization -> Flux Restore -> GFPGAN v1.4 scale 1 -> Sharp Master/Preview/2HD/4HD -> R2. Standard is unchanged by Premium foundations.

## Storage Mapping

- Original: `RestorationItem.originalStorageKey` (`originals/`).
- Flux/GFPGAN: in-memory provider outputs; prediction metadata only, no standalone R2 intermediate key.
- Master/final: `finalStorageKey` and `metadata.restorationOutputs.master.key` (`finals/`).
- Preview: `previewStorageKey` (`previews/`).
- 2HD/4HD: `metadata.restorationOutputs.variants` (`finals/`).

## Comparison, Entitlement, Payment

Before is the signed original key; After is signed final key. Missing original shows an error. Downloads require guest/user ownership plus explicit purchased tier. Payment alone does not unlock a legacy tier. No provider processing or processed preview before confirmed payment.

## Disabled/Proposed

Damage masks are Sharp-decoded grayscale PNGs and disabled by `RESTORATION_DAMAGE_MASK_ENABLED=false`. Face gate is disabled by `RESTORATION_FACE_GATE_ENABLED=false`. Premium Reconstruction is a separate disabled contract; it requires validated mask, preservation instruction, payment, and quality acceptance. `UnifiedLocalRestorationProvider` is unsafe/quarantined.

Premium routing is Standard at 0-39, Standard or Premium at 40-79, and Premium-only at 80-100. Severe face loss or excessive mask coverage is Premium-only. Calibration evidence remains incomplete.

Calibration intake: run `npm run restoration:fixture-intake -- --id <id> --category <category> --original <path>` with optional `--mask`, `--flux`, `--gfpgan`, and `--final`. Intake validates local image decoding/checksums but does not infer categories or identity metrics.

Run `npm run test:restoration:review-queue` to generate a local operator contact sheet and evidence queue. Only trace-backed groups are registered; filename-only candidates remain ungrouped.

RunPod is disabled by default and benchmark-only. The isolated worker supports health and local dry-run metadata; it is not connected to Standard, Premium, or production routing.

Gate 1 remains build-only validation and does not publish images. Gate 2 is explicitly approved once per run for one development-only immutable GHCR image, via manual workflow dispatch on the default branch, using the validated full SHA and registry digest evidence. Gate 2 was consumed on 2026-07-31 by publishing the GPU candidate as `ghcr.io/ai-photo-studio/ai-photo-studio/runpod-worker-gpu-dev:f65088b5f6bb2f5a91b8b877b32f032766c8b5f1` at digest `sha256:049a304b44bec75562a74eac3f5be312feacd6133da80a0dc86d0a136a86a63a` (source SHA `f65088b5f6bb2f5a91b8b877b32f032766c8b5f1`, subtree `ea8a583e5d7279c0b67eec66a1906b7523c4ce99`); publicationAllowed is now false and not reusable; any candidate change requires a new explicit Gate 2 approval. Gate 3 canary readiness references the published handler image digest `sha256:1a74aefec1a7f77ebdbf7fd19ba2b9a816600f1e3d43ac7ce10b3b87367a3895`; the registry access is resolved as PUBLIC (anonymous pull succeeded, run 30652207024) and a deterministic synthetic canary fixture is verified offline (run 30653164933; PNG `f4368b08...`, input `b2e6c6a2...`, output `99def1c3...`, faces=1). The Gate 3 canary remains BLOCKED only on the GPU/volume region coexistence decision (`regionCompatibilityResolved: false`): read-only RunPod authorization was provided but `RUNPOD_API_KEY` is not set in the environment, so no read-only GET/list query could be authenticated (credential-unavailable; no key extracted from git/committed files). A separate unpublished handler-wrapper candidate now exists at `apps/api/runpod-worker-gpu-serverless-dev/` (`runpod.serverless.start` wrapper around the immutable CLI image); it is build-test only, contains no weights, and requires a new explicit Gate 2 approval before publication. Wrapper Gate 2 readiness is prepared (source frozen at `21e2921`, subtree `b9402fa9...`); publication must pin the exact source/subtree and one immutable tag, and the registry digest must be verified before Gate 2 closes. No model weights may be published; publication is not GPU or quality approval. Wrapper Gate 2 was CONSUMED on 2026-07-31 (published as `ghcr.io/ai-photo-studio/ai-photo-studio/runpod-worker-gpu-serverless-dev:21e292103979f0450dffafe09844fac3b435031b` at digest `sha256:1a74aefec1a7f77ebdbf7fd19ba2b9a816600f1e3d43ac7ce10b3b87367a3895`); the wrapper publicationAllowed is now false and not reusable; any wrapper change requires a new explicit Gate 2 approval. Gate 3 RunPod canaries and Gate 4 production activation remain prohibited unless separately approved.

Legacy coupled RunPod tests are superseded by current-main-native tests. Gate 3 cannot start from documentation alone; explicit user approval, verified rate, fixed budget, endpoint/template, and one-job limit are mandatory. Manifest defaults remain fail-closed. No secret may be committed. The RunPod template must pin the immutable digest. Gate 4 remains separately prohibited. Replicate remains production. Successful health/dry_run alone is not restoration-quality approval.

GFPGANv1.4 weight provenance is verified via GitHub Actions (run 30618746285): size 348632874 bytes, SHA-256 e2cd4703ab14f4d01fd1383a8a8b266f9a5833dacee8e6a79d3bf21a1b6be5ad, checksum source independently-calculated. Apache-2.0 applies to source code only; it does not prove weight redistribution rights. No model binary may be committed; no weight may be bundled without explicit redistribution evidence; external weights must match the pinned checksum before startup; runtime network weight download remains prohibited. A separate unpublished GFPGAN GPU candidate exists at `apps/api/runpod-worker-gpu-dev/` (build-test only) that mounts the weight externally and validates size and SHA-256 before model load; it is not published and GPU execution is unverified. Its build-only CI passed baseline (run 30620758562, image ID sha256:bf6af925ca2d4e3ef9c877a5fcde84907f30ad917e17f0e5591ef907081a8846, baseline size 5522182156 bytes, PyTorch 2.1.2+cu121). The candidate was hardened for Gate 2 readiness: base image pinned by immutable digest, non-root runtime user, build tools removed, caches purged, OCI labels added, SBOM and vulnerability scan performed (findings only; no upload). Hardened CI run 30622042430: size 5454210979 bytes (baseline 5522182156), SBOM 223 packages, vuln scan 35 (MEDIUM 15, HIGH 19, CRITICAL 1) with exact critical blocker CVE-2025-32434 in torch 2.1.2 (fixed in torch >= 2.6.0); not silently ignored. A minimal security upgrade to torch 2.6.0 + torchvision 0.21.0 was investigated and REJECTED (evidence run 30624931147): torchvision 0.21 removed `functional_tensor`, and BasicSR 1.4.2 (latest release) imports it, so the official gfpgan/basicsr stack is incompatible without an unreleased BasicSR pin or a prohibited patch. The official BasicSR fix commit `8d56e3a0` (PR #650) was then evaluated as a reproducible candidate (workflow run 30625934026): it resolves the `functional_tensor` import and GFPGANer constructs under torch 2.6.0+cu126 / torchvision 0.21.0+cu126, but adoption is NOT recommended because that commit is 18 commits / 41 files ahead of v1.4.2 (unrelated substantive changes; the wheel is not minimal) and GFPGANer construction triggers runtime network downloads of facexlib weights. Source archive SHA-256 `88a422325c7a08a9f3b6109e747bef5fbdf85d884d6033eacaf11f6c374aade9`; licence Apache-2.0. The candidate remains on torch 2.1.2+cu121 with CVE-2025-32434 documented. Weight remains external and checksum-pinned. No image has been published; floating tags and checksum drift are prohibited. Build success is not GPU inference or quality approval. The CPU worker remains unchanged. Separate explicit Gate 2 publication approval is mandatory; after publication, separate Gate 3 approval is mandatory. A tracked explicit one-line BasicSR v1.4.2 patch (functional_tensor->functional) was evaluated (run 30627778687): patch SHA-256 834cf12b1e625ce59ec2af4152421d2c6c113b8f0a54ba4544f8937b014289ac, source archive f23fe3558fff4cc038186ffd417d69d8bc1fd0eea2a9c755c401e9dfecc18152; it resolves the import and GFPGANer constructs under torch 2.6.0+cu126/torchvision 0.21.0+cu126, but the wheel build is blocked on a CUDA-less runner and GFPGANer still runtime-downloads facexlib auxiliary weights (detection_Resnet50_Final.pth, parsing_parsenet.pth). Both auxiliary weights have independent SHA-256 only (no publisher digest) and redistribution is unverified. Candidate dependencies remain unchanged; adoption not approved; runtime download and weight bundling prohibited. Fully offline GFPGAN construction and safe loading of all three externally mounted, checksum-verified weights was proven in a `--network none` container (workflow run 30632558271, `GFPGANer constructed; main GFPGAN weight loaded`, tiny CPU contract inference faces: 1, `gpu_inference_executed: false`). Independent hashes are technical integrity pins, not publisher-signed and not redistribution permission; external/BYO mounting is the only candidate packaging mode. `offlineConstructionVerified: true`; candidate unchanged; adoption not approved; publication, runtime download and weight bundling remain prohibited. The proven torch 2.6 stack was adopted into the unpublished candidate (torch 2.6.0+cu126 / torchvision 0.21.0+cu126, gfpgan 1.3.8, facexlib 0.3.0, and BasicSR v1.4.2 source f23fe355... with tracked patch 834cf12b... applied at build). The worker validates all three external weights (main + detection + parsing) and enforces TORCH_FORCE_WEIGHTS_ONLY_LOAD=1. Candidate adopted but unpublished; build/CPU inference is not GPU-quality approval; separate Gate 2 publication approval and later Gate 3 canary approval remain mandatory. Gate 3 and Gate 4 remain prohibited. Candidate source is frozen pending the Gate 2 decision; any candidate code/dependency change invalidates the current Gate 2 evidence and requires a new explicit Gate 2 approval. Any future publication must pin an immutable source SHA and a single immutable tag (no latest/dev floating tag); the published digest must be verified before Gate 2 closes; weights must never be bundled; publication is not GPU execution or quality approval.

A read-only, build-test-only mount-path audit (`.github/workflows/mount-path-audit.yml`, run `30659523185`) proved that the currently published handler image (digest `sha256:1a74aefec1a7f77ebdbf7fd19ba2b9a816600f1e3d43ac7ce10b3b87367a3895`) cannot load weights from `/runpod-volume`, the RunPod Serverless Network Volume mount point: synthetic fixtures mounted there were invisible via `/models` (`weightPresent`/`auxDetectionPresent`/`auxParsingPresent` all `false`), while a control mount directly at `/models` succeeded, and no `/runpod-volume` reference exists anywhere in the worker/handler source. A separate unpublished correction candidate, `apps/api/runpod-worker-gpu-serverless-volume-dev/`, was created, frozen at source commit `158bbbcb82b02db0ca0a2e66bc88a2ccfdb6e374` (subtree `716fc09fc35bb966637bef26f3d4840a7088891b`), deriving directly from the same published handler digest and adding exactly one build-time change: a fixed symlink `/models -> /runpod-volume/models` (no init script, no handler/CMD/entrypoint/dependency change). Build-only CI (run `30660299770`, confirmed via `gh run view --json headSha` to have tested this exact frozen commit) passed all mount-contract tests (valid mapping, missing-volume fail-closed, invalid-weight fail-closed, non-root security), zero CRITICAL, CVE-2025-32434 absent, SBOM 324 components, image ID `sha256:d66479f1e8e009b7379ac32a30d6c06789f6d687b6cb0a1cdff0be7c935ddc1d`, size `6751978537` bytes. Gate 2 readiness packet was created for this candidate (`RUNPOD_VOLUME_HANDLER_GATE2_APPROVAL_PACKET.md`, `runpod-volume-handler-gate2-readiness.json`). Volume-mapped handler Gate 2 was APPROVED (explicit user authorization, 2026-07-31T22:51:08Z) and CONSUMED by publication (workflow runs 30671211535 + fix + 30672004615). Published as `ghcr.io/ai-photo-studio/ai-photo-studio/runpod-worker-gpu-serverless-volume-dev:158bbbcb82b02db0ca0a2e66bc88a2ccfdb6e374` at digest `sha256:29ca5aa0aae46ab03719c52ae25fa98a61830adbbc8b317bd244ffb7ff837d9b`. All post-publication verification tests PASS (non-root user, symlink target verified inside container, handler path, no weights). `publicationAllowed` is now `false` and not reusable; any source/base/dependency change requires a new explicit Gate 2 approval. Network Volume creation requires separate, explicit mutation/cost authorization not covered by any publication approval. Under one-time dispatch authorization, `runpod-create-gate3-volume.yml` was dispatched exactly once (run `30674708510`, 2026-08-01T00:07:14Z) and FAILED at the preflight step; the Create Network Volume step and everything after it show `status: skipped`, so no `POST /v1/networkvolumes` call was made and no RunPod resource, endpoint, template, worker, or job of any kind was created (compute cost $0.00). Root cause: the preflight step used an obsolete GraphQL contract (a nonexistent root `datacenters` field and an invalid `gpuCount` argument on `gpuTypes`), a workflow-implementation defect, not an authorization or credential issue. This was corrected in a narrow follow-up PR: datacenter/GPU discovery now uses the current official `runpodctl` CLI (`runpodctl datacenter list`, `runpodctl gpu list --include-unavailable`) installed from a pinned GitHub release binary, and the volume-creation request body was corrected to match the official `NetworkVolumeCreateInput` schema (`name`, `size`, `dataCenterId` only, resolved dynamically from preflight — no hardcoded/unverified datacenter literal, no unsupported `tier` field). Under a second, separate, explicit dispatch authorization, the corrected workflow was dispatched exactly once (run `30675847067`, 2026-08-01T00:34:03Z) and again FAILED at preflight (`Storage-capable datacenters found: 0`, `Compatible 16GB-class GPU types found: 5`); the Create Network Volume step and everything after it again show `status: skipped` — no `POST /v1/networkvolumes` call was made, no RunPod resource of any kind was created, compute cost $0.00. This authorization is consumed; no third dispatch is authorized. Root cause classified `PARSER_DEFECT`, proven by direct inspection of the `runpodctl` v2.8.0 source: `runpodctl datacenter list` and `runpodctl gpu list` structurally never return a `storageSupport`/`supportNetworkVolume`-style field (their Go structs only carry `id`/`name`/`location`/`gpuAvailability` and `gpuId`/`displayName`/`memoryInGb`/.../`dataCenterAvailability`, respectively) — the preflight's filter for such a field was checking data that could never be present, and silently treated that absence as a negative result rather than an unknown. This is proof of a workflow bug, not proof that the RunPod account lacks an eligible datacenter. The parser was corrected in a second narrow PR to cross-reference GPU/datacenter compatibility using only the documented `dataCenterAvailability` field, with deterministic schema fixtures added; storage/Network-Volume capability per datacenter remains genuinely unverifiable through any documented read-only interface currently available, so the corrected preflight now fails with an honest "cannot be verified" message instead of a fabricated negative result, and continues to refuse to guess a target datacenter. Classification of whether an eligible datacenter exists for this account was `UNPROVEN`; a RunPod support request was drafted (not sent) to resolve it, and a weight-delivery architecture fallback comparison was prepared (not implemented). This was resolved by manual RunPod console evidence (Create Network Volume UI, GPU filter "NVIDIA RTX 4000 Ada Generation", 2026-08-01): EU-RO-1 was selectable (Low availability, Global Networking, S3), alongside EUR-IS-1 and US-CA-2 (N/A); EU-RO-1 was selected. The workflow was corrected to this fixed, console-proven configuration (10GB, EU-RO-1, RTX 4000 Ada, $0.70/month cap; PR #74, commit `2872e37`, merge `28b8e7d`), retaining a live re-verification step that cross-references the fixed pairing against `runpodctl gpu list`'s documented `dataCenterAvailability` field and stops before any mutation on contradiction. Under a third, separate, explicit dispatch authorization scoped to this exact configuration, the corrected workflow was dispatched exactly once (run `30677597137`, 2026-08-01T01:17:52Z) and **succeeded**: live evidence confirmed RTX 4000 Ada availability in EU-RO-1 (1 matching entry), and exactly one Network Volume was created (`photo-restoration-gate3-models`, 10GB, EU-RO-1, redacted ID `d6a4504x...`), verified by post-creation GET (size and datacenter both matched expected). Storage cost: $0.70/month maximum. Compute cost: $0.00. No endpoint, template, worker, or job was created; no weights were uploaded. This third-dispatch authorization is consumed; no fourth dispatch is authorized; volume resize/deletion requires new authorization. `regionCompatibilityResolved` is now `true`. This does not resolve Gate 3 by itself: weights are not uploaded (requires separate authorization) and no separate Gate 3 approval statement has been given — Gate 3 remains BLOCKED on those two independent grounds. The mount-path-compatible handler correction is already published and its Gate 2 approval is consumed (see above); it is not a remaining blocker and Gate 2 is not reopened absent a source/base/dependency change. Gate 3 and Gate 4 remain prohibited; Replicate remains production.

## RunPod Restoration Track Roadmap (as of 2026-08-01, evidence: this document's Gate history above)

- **Completed:** CPU/dev worker Gate 2 published and consumed (`runpod-worker-gpu-dev`, digest `sha256:049a304b...`). GPU CLI worker Gate 2 published and consumed (digest `sha256:049a304b44bec75562a74eac3f5be312feacd6133da80a0dc86d0a136a86a63a`). Handler wrapper Gate 2 published and consumed (digest `sha256:1a74aefec1a7f77ebdbf7fd19ba2b9a816600f1e3d43ac7ce10b3b87367a3895`). Volume-mapped handler Gate 2 published and consumed (digest `sha256:29ca5aa0aae46ab03719c52ae25fa98a61830adbbc8b317bd244ffb7ff837d9b`, publication runs 30671211535+30672004615, all post-publication tests PASS). GHCR registry access resolved PUBLIC. Synthetic canary fixture verified offline. **Network Volume created and region/GPU co-location proven** (run `30677597137`: `photo-restoration-gate3-models`, 10GB, EU-RO-1, `regionCompatibilityResolved: true`). Mount-path audit of the original handler digest completed (that digest was incompatible); the volume-mapped correction candidate resolves this and is itself published with Gate 2 consumed (digest `sha256:29ca5aa0aae46ab03719c52ae25fa98a61830adbbc8b317bd244ffb7ff837d9b`) — mount-path compatibility is fully resolved, not a remaining blocker.
- **Current stage:** Volume-mapped handler candidate Gate 2 remains APPROVED, PUBLISHED, and CONSUMED (one-time, not reusable). Network Volume creation succeeded on the third authorized dispatch (run `30677597137`) after two failed dispatches (`30674708510` obsolete GraphQL; `30675847067` parser defect; both corrected, both created nothing) and resolution of the `UNPROVEN` datacenter-capability question via manual RunPod console evidence. This third-dispatch authorization is consumed; no fourth dispatch is authorized.
- **Remaining stages, in order:** (1) separate, explicit authorization to upload weights to the created Network Volume; (2) explicit, separate Gate 3 approval for a single one-job canary with fixed budget and fail-closed limits; (3) canary execution and evidence capture; (4) Gate 4 (production activation) remains separately prohibited and is not currently planned. (Mount-path compatibility is already resolved — the correction candidate is published and Gate 2 is consumed; no further work is needed there.)
- **Approximate completion:** ~80% of the path to a Gate-3-ready state — up from ~75%, now that the Network Volume has been successfully created and verified (`photo-restoration-gate3-models`, 10GB, EU-RO-1) and region/GPU co-location is proven (`regionCompatibilityResolved: true`). All build/publication work, both rounds of workflow-repair, and volume creation are complete. 0% of Gate 3 or Gate 4 execution has occurred.
- Protected protocol correction: the volume-mapped handler publication contradiction is resolved; its Gate 2 approval is APPROVED/PUBLISHED/CONSUMED at digest `sha256:29ca5aa0aae46ab03719c52ae25fa98a61830adbbc8b317bd244ffb7ff837d9b`, and no fresh Gate 2 is required unless source, base, or dependencies change. Upload stopped safely before mutation. RunPod S3 credentials and RunPod API credentials are separate; Cloudflare R2 credentials must never be reused as RunPod credentials. No weight is accepted without exact size and SHA-256 verification. Downloading replacement weights requires separate authorization, credential creation requires manual user action, and upload requires new explicit authorization. Gate 3 and Gate 4 remain prohibited; Replicate remains production.
- Authorized workflow run `30690211053` uploaded and independently verified exactly three approved external weights in `d6a4504x8m/models/` at EU-RO-1. This establishes technical weight readiness only; Gate 3 remains `approved=false` and requires separate written approval before any endpoint, worker, job, GPU execution, or production routing.
- **Gate 3 canary executed and consumed (2026-08-01).** Explicit user approval authorized exactly one non-production RunPod Serverless canary. Workflow `.github/workflows/runpod-gate3-one-canary.yml` was added (PR #81) and dispatched three times: run `30691165812` failed at a preflight git-ancestry check (shallow clone; fixed in PR #82; zero RunPod calls, $0.00); run `30691290437` created and cleanly deleted one template and one endpoint but never reached job submission (shell argument-length limit passing the base64 fixture; fixed in PR #83; zero jobs, $0.00); run `30691401701` created one template and one endpoint, submitted and executed exactly one restore job on a live Flex GPU worker (`delayTime` 51439ms, `executionTime` 10747ms), and the job reported `status: FAILED` with no decodable output (`gpu_inference_executed: false`, `providerPostCount` absent from the error-shaped output). Actual compute cost was `$0.001719`, within the `$0.05` budget. Cleanup was independently verified in all three runs: endpoint and template both deleted (HTTP 204), no active workers remained, and the Network Volume and its weights were never touched. **This job execution consumes the Gate 3 approval; no further canary may run without a new, separate, explicit Gate 3 approval statement.** A plausible, code-derived (not independently confirmed) root cause is recorded in `runpod-gate3-readiness.json` (`gate3CanaryAttempt.suspectedRootCause`): the canary template's env object was empty, and `worker.py` requires `TORCH_FORCE_WEIGHTS_ONLY_LOAD=1` before it will load any weight.
- **Offline root-cause analysis (2026-08-01, no RunPod call, no dispatch).** The env hypothesis above was investigated using only read-only repository/image inspection (published image `sha256:29ca5aa0aae46ab03719c52ae25fa98a61830adbbc8b317bd244ffb7ff837d9b` pulled anonymously by digest via the GHCR registry API) and is classified **CONTRADICTED**: the image's `Config.Env` already bakes in `TORCH_FORCE_WEIGHTS_ONLY_LOAD=1`, and `handler.py`'s `subprocess.run()` call passes no `env=` override, so the worker subprocess inherits it regardless of the RunPod template's own env configuration. See `docs/restoration/runpod-gate3-canary-image-env-evidence.json` for the full evidence record. The real cause of the `FAILED` job remains **unconfirmed** — cleanup deleted the job before any log-retrieval call was authorized, and RunPod's job status API exposes no stdout/stderr endpoint. `.github/workflows/runpod-gate3-one-canary.yml` was corrected defensively anyway: the canary template now explicitly sets exactly `TORCH_FORCE_WEIGHTS_ONLY_LOAD=1` with a pre-submit assertion proving the template response reflects it, and pre-cleanup sanitized diagnostics (bounded, secret-free) were added to capture more of a future job's own JSON output/error fields. The workflow's header now explicitly documents that any future Gate 3 approval authorizes exactly one dispatch, one job, zero reruns, and that any preflight/workflow defect consumes that dispatch's authorization. Gate 3 remains `approved=false` and a new, separate, explicit approval is required before any further RunPod call. Gate 4 remains prohibited; Replicate remains production.
- **Second Gate 3 canary dispatch, corrected workflow (2026-08-01).** A second, separate, explicit user approval authorized exactly one new canary dispatch of the corrected workflow (PR #85, merge `8c4db06`). Run `30692613631`: all preflights passed, including the new pre-submit assertion confirming the template response reflects `TORCH_FORCE_WEIGHTS_ONLY_LOAD=1`; template and endpoint were created; exactly one job was submitted and executed on a live Flex GPU worker (`delayTime` 51978ms, `executionTime` 9911ms, billed 62s); the job again reported `status: FAILED` with no decodable output. Actual compute cost `$0.001585`, within budget. The improved sanitized diagnostics captured, for the first time, `errorDetail: "worker exited with unexpected code 1"` — `worker.py`'s catch-all `EXIT_INTERNAL` code for an unclassified/uncaught exception, which is distinct from its `EXIT_WEIGHT` (4) and `EXIT_CUDA` (5) codes. This positively confirms the failure is **not** a weight-validation or CUDA-availability failure, further corroborating the offline `CONTRADICTED` classification of the env-var hypothesis. The exact Python exception/traceback remains unavailable (`worker.py` writes it only to its own stderr, which `handler.py` never surfaces in the JSON response or the RunPod job API). Cleanup was independently verified again: endpoint and template both deleted (HTTP 204), no active workers remained, Network Volume and weights untouched. **This second approval is now consumed; no further canary may run without a new, separate, explicit Gate 3 approval statement.** Gate 3 remains `approved=false`. Gate 4 remains prohibited; Replicate remains production; no production routing occurred.
- **EXIT_INTERNAL root cause confirmed offline (2026-08-01, no RunPod call, no GPU required).** `worker.py`'s `_run_restore()` unpacked `GFPGANer.enhance()`'s return value into 2 variables (`out, _ = model.enhance(...)`), but `enhance()` — verified against the exact pinned `gfpgan==1.3.8` tag source — always returns a 3-tuple `(cropped_faces, restored_faces, restored_img)`. This raises an uncaught `ValueError: too many values to unpack (expected 2, got 3)` on every successful restore call, propagating to `worker.py`'s generic `except Exception: sys.exit(EXIT_INTERNAL)` handler — exactly matching both canary failures' `exit code 1`. Reproduced empirically (no GPU/torch/RunPod needed) by monkeypatching `validate_weight`/`cuda_info`/`load_model` and calling the real `_run_restore()` with a stub `model.enhance()` returning a verified-shape 3-tuple. `facexlib==0.3.0`'s zero-detected-face handling was separately verified as non-throwing (ruled out as a contributing factor, given the canonical fixture has no real face). Full record: `docs/restoration/runpod-restore-unpack-fix-candidate.json`. **Classification: `ROOT_CAUSE_CONFIRMED`.** The one-line fix (`_, _, out = model.enhance(...)`) is applied ONLY in a new, unpublished candidate at `apps/api/runpod-worker-gpu-dev-restore-unpack-fix/`; the currently approved/published immutable CLI base digest (`sha256:049a304b44bec75562a74eac3f5be312feacd6133da80a0dc86d0a136a86a63a`) is unchanged and not reopened. Publishing this candidate requires a fresh, separate, explicit Gate 2 review. Both 2026-08-01 Gate 3 approvals remain consumed; Gate 3 remains `approved=false`; Gate 4 remains prohibited; Replicate remains production.
- **Complete corrected chain built and Gate-2-reviewed offline (2026-08-01, build-only, no publication, no RunPod call).** The corrected CLI candidate alone is insufficient: the currently published Serverless handler and volume-mapped handler images both derive from the OLD, buggy CLI digest and would not inherit an unpublished CLI fix. Two more unpublished candidates complete the chain: `apps/api/runpod-worker-gpu-serverless-restore-unpack-fix-dev/` (unchanged handler contract, derives from the corrected CLI candidate by local build reference) and `apps/api/runpod-worker-gpu-serverless-volume-restore-unpack-fix-dev/` (adds only the build-time `/models -> /runpod-volume/models` symlink, derives from the corrected Serverless handler by local build reference). A new build-only workflow, `.github/workflows/build-restore-unpack-fix-chain.yml`, builds all three in dependency order using local image tags (no registry involved, since none of the three are published) and runs the full contract-test/SBOM/vulnerability-scan suite per image. First dispatch (run `30693830028`) failed at the Serverless-handler stage on a `docker/setup-buildx-action` isolated-builder image-resolution defect (Stage 1/CLI itself fully passed; zero images published, zero cost); fixed narrowly (PR #89, merge `d38c867`). Second dispatch (run `30694219808`) **succeeded end to end**: all three images built, non-root `workeruser` runtime confirmed throughout, `ENTRYPOINT`/`CMD` correct at each stage, zero CRITICAL vulnerabilities and CVE-2025-32434 absent for all three (SBOM: 248 / 324 / 324 components), the one-line unpack fix confirmed present (and the old buggy line confirmed absent) in the CLI image, and the full mount-contract test suite (valid mapping, missing-volume fail-closed `EXIT_WEIGHT=4`, invalid-weight fail-closed, symlink not runtime-writable, `providerPostCount=0`/`productionRoutingAllowed=false`) passed on the final volume-mapped image, which was independently confirmed to carry the corrected `worker.py` end to end (rejecting the old buggy digest). Full readiness record: `docs/restoration/runpod-restore-unpack-fix-chain-gate2-readiness.json`. **This is a build-only readiness review, not an approval**: `approved=false`, `publicationAllowed=false` for all three proposed images, `expectedDigest` empty for all three until publication. Publishing only a subset of the chain is explicitly insufficient and documented as such; any source/base/dependency change to any of the three candidates invalidates the entire chain's readiness. Publication requires a separate, explicit Gate 2 approval; a subsequent, separate, explicit Gate 3 approval (referencing the new final volume-mapped digest specifically) would then be required before any further canary. Both 2026-08-01 Gate 3 approvals remain consumed. Gate 4 remains prohibited; Replicate remains production; no production routing occurred.

## RunPod Restoration Track Roadmap Update (2026-08-01)

- **Root-cause/diagnostic path: ~99% complete.** The exact defect is confirmed (one-line `GFPGANer.enhance()` unpack fix), verified against upstream source, reproduced empirically offline, and dependency/ABI-audited. Nothing further is planned here.
- **Successful-canary path: ~94% complete.** Remaining stages, in order: (1) a separate, explicit Gate 2 approval to publish the corrected three-image chain (CLI, Serverless handler, volume-mapped handler), each pinned by full source SHA with no floating tag; (2) publication itself (build + push, out of scope for this review); (3) post-publication verification of each published digest (non-root, entrypoint/cmd, no bundled weights, anonymous-pull check — mirroring prior publication verification patterns); (4) a new, separate, explicit Gate 3 approval for exactly one new non-production canary using the new final volume-mapped digest; (5) canary execution and evidence capture; (6) if successful, no further stage is currently planned — Gate 4 (production activation) remains separately prohibited regardless.
