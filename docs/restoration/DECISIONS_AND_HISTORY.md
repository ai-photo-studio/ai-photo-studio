# Decisions And History

- `8375dac11a9b1605de72dbd35b3b0cfb572a2e7a`: split result and print flow.
- `598ef52ffc38375b55d1324f85b41efa9021c233`: validated download bytes and refreshed homepage.
- `4ea54a189bf74be92be0891868077c124f547623`: original/final comparison mapping and explicit paid tiers.
- `3078c9cdbf2dcabf9d6f5e82c6aa57214d8f76ff`: isolated structural mask foundation and quarantined legacy local provider.
- `f33d0948d25f090e364a3dbc0dd6b6db79defc3b`: disabled face-restoration scoring foundation.

Rejected: Unified local restoration because one endpoint may rerun inpainting, face, color, and upscale transforms. Unresolved: offline detector/embedding calibration and verified legacy tier backfill.

Premium routing was revised to 0-39 Standard, 40-79 Standard or Premium, and 80-100 Premium-only; this is a disabled policy foundation pending archived calibration evidence.

Face embeddings and landmarks remain unavailable: no local face-analysis dependency is installed. They must remain unavailable rather than synthetic until an approved benchmark-only adapter is supplied.

OPS-113 is currently the only trace-backed review candidate. Other archived stage-like directories remain ungrouped pending operator evidence.

RunPod A4000 Serverless is approved for controlled development/benchmarking only, with active workers 0 and maximum Flex workers 1. No remote call occurred.

Gate 1 stays build-only and non-publishing. Gate 2 received a one-run explicit approval for exactly one immutable development GHCR image published from the validated full SHA and verified by digest. Gate 2 was consumed on 2026-07-31 by publishing the GPU candidate at digest `sha256:049a304b44bec75562a74eac3f5be312feacd6133da80a0dc86d0a136a86a63a`; publicationAllowed is false and not reusable; any candidate change requires a new explicit Gate 2 approval. A Gate 3 canary referencing the published handler digest `sha256:1a74aefec1a7f77ebdbf7fd19ba2b9a816600f1e3d43ac7ce10b3b87367a3895` has registry access resolved as PUBLIC (anonymous pull succeeded, run 30652207024) and a verified synthetic canary fixture (run 30653164933); it remains BLOCKED only on the GPU/volume region coexistence decision (`regionCompatibilityResolved: false`). A separate unpublished handler-wrapper candidate exists at `apps/api/runpod-worker-gpu-serverless-dev/`; it is build-test only and requires a new explicit Gate 2 approval before publication. Wrapper Gate 2 readiness is prepared (source `21e2921`, subtree `b9402fa9...`); publication must pin the exact source/subtree and one immutable tag with digest verification before closure. Wrapper Gate 2 was CONSUMED on 2026-07-31 (published at digest `sha256:1a74aefec1a7f77ebdbf7fd19ba2b9a816600f1e3d43ac7ce10b3b87367a3895`); wrapper publicationAllowed is now false and not reusable. Gate 3 and Gate 4 remain prohibited unless separately approved. Replicate remains active production.

Legacy coupled RunPod tests are superseded by current-main-native tests. Gate 3 cannot start from documentation alone; explicit user approval, verified rate, fixed budget, endpoint/template, and one-job limit are mandatory. Manifest defaults remain fail-closed. No secret may be committed. The RunPod template must pin the immutable digest. Gate 4 remains separately prohibited. Replicate remains production. Successful health/dry_run alone is not restoration-quality approval.

GFPGANv1.4 weight provenance verified via GitHub Actions run 30618746285: size 348632874 bytes, SHA-256 e2cd4703ab14f4d01fd1383a8a8b266f9a5833dacee8e6a79d3bf21a1b6be5ad, checksum source independently-calculated (API digest absent). Apache-2.0 covers source code only and does not prove weight redistribution rights. No weight is bundled; recommended packaging is externally-mounted-weight with pinned checksum validation. Runtime network weight download is prohibited. A separate unpublished GFPGAN GPU candidate (`apps/api/runpod-worker-gpu-dev/`) was created for build and contract tests only; it uses an externally mounted weight and fails closed on size/checksum mismatch and on missing CUDA. Its baseline build-only CI passed (run 30620758562, image ID sha256:bf6af925ca2d4e3ef9c877a5fcde84907f30ad917e17f0e5591ef907081a8846, PyTorch 2.1.2+cu121). The candidate was hardened for Gate 2 readiness (base pinned by immutable digest, non-root user, build tools removed, caches purged, OCI labels, SBOM + vulnerability scan). The candidate is not published and GPU execution is unverified. A minimal security upgrade to torch 2.6.0 + torchvision 0.21.0 was investigated and REJECTED (evidence run 30624931147): torchvision 0.21 removed `functional_tensor` and BasicSR 1.4.2 (latest official release) imports it, so the official gfpgan/basicsr stack is incompatible without an unreleased BasicSR pin or a prohibited patch. The official BasicSR fix commit `8d56e3a0` (PR #650) was evaluated as a reproducible candidate (workflow run 30625934026): it resolves the `functional_tensor` error and GFPGANer constructs under torch 2.6.0+cu126 / torchvision 0.21.0+cu126, but adoption is NOT recommended because it is 18 commits / 41 files ahead of v1.4.2 (unrelated changes) and GFPGANer runtime-downloads facexlib weights. Source archive SHA-256 `88a422325c7a08a9f3b6109e747bef5fbdf85d884d6033eacaf11f6c374aade9`; Apache-2.0. CVE-2025-32434 in torch 2.1.2 remains documented, not suppressed. New explicit Gate 2 approval required before any GPU image publication; after publication, separate Gate 3 approval is mandatory. Never bundle weights without explicit redistribution approval; never bypass checksum validation; never replace the verified checksum without new provenance evidence; no floating tags or checksum drift. A tracked explicit one-line BasicSR v1.4.2 patch was evaluated (run 30627778687; patch SHA-256 834cf12b1e625ce59ec2af4152421d2c6c113b8f0a54ba4544f8937b014289ac, source f23fe3558fff4cc038186ffd417d69d8bc1fd0eea2a9c755c401e9dfecc18152): it resolves the torchvision 0.21 functional_tensor import and GFPGANer constructs, but the wheel build is blocked and GFPGANer runtime-downloads facexlib auxiliary weights (independent SHA-256 only, redistribution unverified). Candidate unchanged; adoption not approved; runtime download and weight bundling prohibited. Fully offline GFPGAN construction and safe loading of all three externally mounted weights was proven in a `--network none` container (run 30632558271; `GFPGANer constructed; main GFPGAN weight loaded`). Independent hashes are integrity pins, not publisher-signed or redistribution permission; external mounting is the only candidate mode. `offlineConstructionVerified: true`; candidate unchanged; adoption not approved. The proven torch 2.6 stack was adopted into the unpublished candidate (torch 2.6.0+cu126 / torchvision 0.21.0+cu126, gfpgan/facexlib, BasicSR v1.4.2 source + tracked patch applied at build; safe-load enforced). Candidate adopted but unpublished; build/CPU inference is not GPU-quality approval; separate Gate 2 publication and Gate 3 canary approvals remain mandatory. Gate 3 and Gate 4 remain prohibited. Candidate source is frozen pending decision; any candidate code/dependency change invalidates the current Gate 2 evidence and requires a new explicit Gate 2 approval. Future publication must pin an immutable source SHA and one immutable tag (no floating tags); published digest must be verified before Gate 2 closes; weights must never be bundled.
