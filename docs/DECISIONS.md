# Decisions

- This task is repository automation/documentation setup only; application business logic and production systems remain unchanged.
- The existing `AI_code_audit_report.md` is preserved as the audit-report convention.
- Repository evidence is separated from uncertain live-state claims.
- No readiness percentages are recorded without direct repository evidence.
- The next task is verification and project-state validation, not feature development.
- Protected authentication, payment, RunPod, AI-provider, deployment, and database areas require explicit authorization before modification.
- R9.3 (2026-08-07): the ThanNow public shell and homepage follow the approved `UI/DESIGN_LOCK.md` locked human-memory direction (Restore -> Upscale -> Print). Homepage section styles are scoped under `.thannow-home` so existing customer/admin flow pages remain unaffected.
- The reference homepage used to present a dual-market "AI Product Photo Studio" positioning; product-photography feature routes remain in the repo but are no longer the primary consumer homepage.
- Missing licensed homepage image assets are a content-completeness item, not a code gate; `RenderAsset` places clean placeholder slots until owned images are supplied.
- R9.3-P1 (2026-08-07): the 16 approved human-memory homepage assets remain absent and are classified **BLOCKED_OWNER_ASSETS**. All non-asset visual/responsive/routing checks passed at 1440/1024/768/430/390/360; placeholders render cleanly and auto-replace once assets are added under `/assets/` (`apps/web/public/assets/`). No fabricated/hotlinked imagery is ever added.
- R9.3-P1 (2026-08-07, asset drop): the 16 approved human-memory assets were supplied to `apps/web/public/assets/` and are now served at `/assets/`. The homepage renders all 16 images (0 placeholders, 0 broken) at desktop and mobile, and the PNG/JPG asset set is packaged into `dist/assets/` on build. The asset blocker is cleared; post-launch polish can proceed.
- R9.3-P2 (2026-08-07, launch prep): website DEPLOY_READY. Removed legacy `apps/web/src/main.tsx` product-studio runtime branding and placeholder GTM/Facebook pixel IDs (now gated behind real build-time env IDs). Verified production build: SPA deep-route fallback, 16 assets resolved, no old branding, PKR truthful. Deployment (push/`wrangler pages deploy`) intentionally deferred pending explicit owner authorization per rules.md.
- R9.3-P5 (2026-08-07, prod deploy): RC `ca7d797` deployed to production for `www.thannow.com` (Cloudflare Pages `ai-photo-studio-frontend`) under explicit owner authorization, R9.3 frontend scope only. Live smoke passed (ThanNow branding, 16 assets, Upload CTA -> `/restore/new`, SPA deep routes, mobile CTA, prod API `https://api.thannow.com`, no placeholders/secrets). Status: PRODUCTION_LIVE.
- R9.4-P1 (2026-08-07, prod customer-flow verification): Live commerce spine verified fail-closed and truthful (upload/draft, preview, server pricing, immutable FixedOrder, payment readiness, idempotent attempt; no fabricated paid state). Marked **PAYMENT_EXTERNAL_BLOCKER = TRUE**: no live Bank Alfalah integration, provider is manual/demo only, `bankAlfalahAdapter` is a fail-closed shell. Flowing to real payment is a protected/external requirement, not a UI bug. Also documented: the commerce FixedOrder spine and legacy processing/download/print spine are not connected, and `RestorePrintPage.tsx` hardcodes fake PKR prices with no data wiring (both require an authorized commercial-build packet).
- R9.3-P7 (2026-08-07, HD rotating hero): the homepage hero comparison was upgraded from the legacy single `hero-compare.png` to 10 rotating before/after hero pairs. Decided to use the HD 1600x1600 source pairs directly (no re-upscale/no blurry CSS stretching), one locked square compare frame with base=Then and clipped overlay=Now, a customer-draggable divider (mouse/touch/pointer, `touch-action:none`), random first hero on mount, auto-rotation ~7s resetting to 50%, pause while dragging, and preload only current+next pair. Assets resolved from `apps/web/public/assets/hero/hero/*.jpg` (the manifest's then/now URLs were corrected to the real `hero/` subpath). CTA/modal and `/restore/new` routing left unchanged.
- R9.3-P10 (2026-08-07, hero quality fix): switched hero comparison from `object-fit: cover` (which cropped photos) to `object-fit: contain` + `object-position: center` with a blurred/darkened same-image background so the FULL photograph is always visible and pixel-aligned. Moved the caption to a strip below the frame and the Upload CTA outside the photo area so they no longer cover faces. Regenerated all 10 `*-then.jpg` deterministically from their exact matching `*-now.jpg` (sharp, seeded, idempotent generator `apps/web/scripts/generate-hero-then.cjs`) each with a DISTINCT realistic damage preset (faded sepia+cracks / B&W dim+aging / torn corners+folds+stains / faded color+water damage / low contrast+dust+yellowing / B&W scratches+cracked emulsion / heavy torn edges+missing corner / aged B&W grain+scratches / severe aging+multi-tears+stains / dim+damaged emulsion+partial fading). Then/Now remain the exact same scene/people/composition at 1600x1600.
- R9.3-P11 (2026-08-07, hero-quality production deploy): owner-authorized push (`72825bc`) + production deploy to `www.thannow.com` (Cloudflare Pages `ai-photo-studio-frontend`, Production deployment `3a52a1f0`, branch `main`, commit `72825bc`). Live smoke all PASS (full photo visible, object-fit contain, blurred bg, pixel-aligned layers, slider left/right, mouse+touch, ~7s rotation resets 50%, caption/CTA outside image, CTA→/restore/new, no overflow across 6 viewports, all 20 assets 200, all 10 distinct damage presets reachable). **Hero stage PRODUCTION_LIVE.**
