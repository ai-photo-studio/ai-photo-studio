[project]
name = "modal-background-remover"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "modal",
    "rembg",
    "pillow",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]